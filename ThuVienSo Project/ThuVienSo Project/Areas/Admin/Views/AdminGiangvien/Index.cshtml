@using PagedList.Core.Mvc
@model PagedList.Core.IPagedList<ThuVienSo_Project.Models.Giangvien>

@{
    ViewData["Title"] = "Giảng viên - page: " + @ViewBag.CurrentPage;
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home"  class="breadcrumb-item" asp-action="Index"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <a class="breadcrumb-item" asp-area="Admin" asp-controller="AdminGiangvien" asp-action="Index">Danh sách Giảng viên</a>
            <span class="breadcrumb-item active">Danh sách Giảng viên : page @ViewBag.CurrentPage</span>
        </nav>
    </div>
</div>
<div class="card">
    <div class="card-body">
        <div class="row m-b-30">
            <div class="col-md-8">
            </div>
            <div class="col-lg-4 text-right">
                <a class="btn btn-primary" asp-controller="AdminGiangvien" asp-action="Create"> <i class="anticon anticon-plus-circle m-r-5"></i>  <span>Thêm mới</span> </a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-hover e-commerce-table">
                <thead>
                    <tr>
                        <th>Mã Giảng viên</th>
                        <th>Tên Giảng viên</th>
                        <th>Email</th>
                        <th>Giới tính</th>
                        <th>Khoa/Bộ môn</th>
                        <th>#</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Magv</td>  
                                <td>@item.Hoten</td> 
                                <td>@item.Email</td>
                                <td>@item.Gioitinh</td>
                                <td>@item.IdkhoaNavigation.Tenkhoa</td>
                                <td>
                                    <a class="btn btn-primary btn-tone m-r-5" asp-area="Admin" asp-controller="AdminGiangvien" asp-action="Details" asp-route-id="@item.Magv">View</a>
                                    <a class="btn btn-secondary btn-tone m-r-5" asp-area="Admin" asp-controller="AdminGiangvien" asp-action="Edit" asp-route-id="@item.Magv">Edit</a>
                                    <a class="btn btn-success btn-tone m-r-5" asp-area="Admin" asp-controller="AdminGiangvien" asp-action="Delete" asp-route-id="@item.Magv">Delete</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
            <div aria-label="Page navigation example">
                <ul class="pagination">
                    <pager class="pager-container" list="@Model" asp-area="Admin" asp-controller="AdminGiangvien" asp-action="Index" />
                </ul>
            </div>
        </div>
    </div>
</div>
